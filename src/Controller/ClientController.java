package Controller;

public class ClientController {

    public boolean connectToServer(){

        // write code for client to connect to sever
        // Send packet object as msg
        return true;
    }
}
